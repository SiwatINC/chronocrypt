# Database Configuration for ChronoCrypt + Prisma

# PostgreSQL (recommended for production)
DATABASE_URL="postgresql://user:password@localhost:5432/chronocrypt?schema=public"

# SQLite (good for development/testing)
# DATABASE_URL="file:./chronocrypt.db"

# Connection pooling (PostgreSQL only)
# DATABASE_URL="postgresql://user:password@localhost:5432/chronocrypt?schema=public&connection_limit=10&pool_timeout=20"
